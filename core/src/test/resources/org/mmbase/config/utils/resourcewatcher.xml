<?xml version="1.0"?>
<!DOCTYPE util PUBLIC "-//MMBase//DTD util config 1.0//EN" "https://www.mmbase.org/dtd/util_1_0.dtd">
<util>
  <properties>
    <!--
         Many resources in MMBase are 'watched' for changes.
         Every of these watched resources has its own check interval (usually hard-coded between a few seconds and a few minutes).
         The one thread which is doing all the checking, does sleep between checks too.
         It's sleep time can be configured here. During development you want it to be low, in production high to avoid the overhead.
    -->
    <property name="delay">PT10S</property> <!--Time in  between checks -->

    <!-- resource watching can be effectively disabled by setting this to a negavtive value, e.g. PT-10s, -->
    <!--
    <property name="resourcewatcher.delay">PT-10s</property>
    -->
    <property name="defaultdelay">PT10S</property>
  </properties>
</util>
